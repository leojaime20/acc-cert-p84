{
  "name": "acc-cert-functions",
  "private": true,
  "main": "lib/index.js",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint src --max-warnings=0",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "archiver": "^8.0.0",
    "firebase-admin": "13.10.0",
    "firebase-functions": "^7.3.0",
    "pdfkit": "^0.17.2"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.17.5",
    "typescript": "^6.0.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
